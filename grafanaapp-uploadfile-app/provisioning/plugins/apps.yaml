apiVersion: 1

apps:
  - type: 'grafanaapp-uploadfile-app'
    org_id: 1
    org_name: 'grafana-app'
    disabled: false
    jsonData:
      apiUrl: http://default-url.com
    secureJsonData:
      apiKey: secret-key
